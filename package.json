{
  "name": "canopy",
  "version": "0.1.0",
  "description": "树状文档索引 + 两阶段检索（BM25 → LLM tree search）的独立 CLI 工具库",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "canopy": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@node-rs/jieba": "^2.0.1",
    "chokidar": "^4.0.0",
    "commander": "^13.0.0",
    "js-tiktoken": "^1.0.15",
    "yaml": "^2.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
