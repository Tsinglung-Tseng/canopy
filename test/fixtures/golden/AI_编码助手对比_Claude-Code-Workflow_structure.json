{
  "doc_name": "AI_编码助手对比_Claude-Code-Workflow",
  "line_count": 20,
  "structure": [
    {
      "title": "AI 编码助手工作流对比",
      "node_id": "0001",
      "line_num": 1,
      "text": "# AI 编码助手工作流对比\n\n本文虚构对比几款编码助手的使用流程，Claude Code 作为命令行代理的代表。",
      "nodes": [
        {
          "title": "Claude Code 的会话模型",
          "node_id": "0002",
          "line_num": 5,
          "text": "## Claude Code 的会话模型\n\nClaude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。 Claude Code 在终端里维持一个持续会话，工具调用与文件编辑都发生在工作目录内。上下文窗口是稀缺资源，长任务需要阶段性总结。",
          "nodes": [
            {
              "title": "权限模式",
              "node_id": "0003",
              "line_num": 9,
              "text": "### 权限模式\n\n默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。 默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。 默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。 默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。 默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。 默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。 默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。 默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。 默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。 默认逐条确认，acceptEdits 自动接受编辑。claude code 的 hooks 可以在工具调用前后注入检查。"
            },
            {
              "title": "子代理",
              "node_id": "0004",
              "line_num": 13,
              "text": "### 子代理\n\n把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。 把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。 把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。 把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。 把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。 把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。 把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。 把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。 把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。 把搜索类任务交给子代理可以保住主窗口的上下文预算。Claude Code 的 Task 工具即为此设计。"
            }
          ]
        },
        {
          "title": "与 IDE 插件的取舍",
          "node_id": "0005",
          "line_num": 17,
          "text": "## 与 IDE 插件的取舍\n\nIDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。 IDE 插件长于内联补全，命令行代理长于多步骤任务编排。两者互补而非互斥。"
        }
      ]
    }
  ]
}